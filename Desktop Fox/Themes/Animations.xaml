<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DesktopFox">
    
    <Storyboard x:Key="ConfirmationLight">
        <ColorAnimation From="{StaticResource Color_Control}" To="LightGreen" 
                        Storyboard.TargetProperty="(Border.Background).Color" 
                        Storyboard.TargetName="{Binding ElementName=Button}" 
                        AutoReverse="True" Duration="0:0:0.3"/>
    </Storyboard>


</ResourceDictionary>